<div class="row">
  <div class="col-md-12 px-0">
    <h1>&nbsp;書類審査申請</h1>
  </div>
</div>

<div class="container">
  <div class="row my-5">
    <%=   "1・2⃣・3" %>
  </div>
  
  <div class="row my-5">
    <%= "本人確認書類(免許書、個人ナンバーカード、学生証※写真付きで住所が確認できる書類の画像)：" %>
  </div>
  
  <div class="row my-5">
    <%= form_with(model: @performer, local: true) do |f| %>
      <%= f.file_field :identification %>
      <%= submit_tag("登録") %>
    <% end %>
  </div>
  
  <div class="row my-5">
    <% if @performer.identification? %>
      <%= image_tag @performer.identification.url, class: 'Horizontal'  %>
    <% end %>
  </div>
  <div class="row my-5">
    <%= button_to "次へ", performers_full_body_path, {method: :get}  %>
  </div>
</div>